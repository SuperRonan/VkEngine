#pragma once

#define COMMA ,

#define FORWARD_MACRO(X) X

#define NOARG

#define BIT_MASK(n) ((1 << n) - 1)

#define SHADER_SEMANTIC_RAY_TRACING_PIPELINE (SHADER_SEMANTIC_RAYGEN || SHADER_SEMANTIC_INTERSECTION || SHADER_SEMANTIC_ANY_HIT || SHADER_SEMANTIC_CLOSEST_HIT || SHADER_SEMANTIC_MISS || SHADER_SEMANTIC_CALLABLE)


#define CAT(X, Y) CAT2(X, Y)
#define CAT2(X, Y) X ## Y


